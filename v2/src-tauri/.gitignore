# Rust build output
/target

# Tauri-generated schemas/code
/gen

# Bundled binaries & large map assets are produced by build steps
/binaries
/resources/map/*.pmtiles
/resources/osrm

# This application lockfile is a reproducible-release input and is committed.
!/Cargo.lock
