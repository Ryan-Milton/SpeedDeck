import ComingSoon from "../ComingSoon";

// Replaced in Phase 7 (local files) and Phase 9 (Spotify).
export default function MusicApp() {
  return <ComingSoon title="Music" phase="Local library & Spotify — Phase 7 / 9" />;
}
