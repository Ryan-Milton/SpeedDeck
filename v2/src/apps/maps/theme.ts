// MapLibre paint needs literal colors (it can't read CSS custom properties).
// These mirror the HUD accent tokens in src/styles.css — keep them in sync.
export const MAP_ACCENT = "#2bd9cc"; // --accent
export const MAP_ACCENT_GLOW = "rgba(43, 217, 204, 0.35)"; // --accent-glow, softer
export const MAP_ROUTE_OUTLINE = "#000000";
